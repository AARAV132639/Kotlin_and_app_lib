#  TCP Client Server Chat Insights

*TCP gives us a reliable byte stream. Java's I/O classes let us turn that byte stream into convernient text operations*

- In Java: ServerSocket --> accept() --> Socket ---> inputStream/OutputStream ---> Reader/Writer

## 1. Differneces: TCP Chat Server/Client in C++ vs Java

## 2. ServerSocket vs Socket

- Server: ServerSocket serverSocket = new ServerSocket(5000);

- *ServerSocket* is the listening endpoint.